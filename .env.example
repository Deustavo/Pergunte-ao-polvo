# LivePix API (server-side only - do not expose to client)
# Create an app at https://livepix.gg/app to get these.
LIVEPIX_CLIENT_ID=your_client_id
LIVEPIX_CLIENT_SECRET=your_client_secret

# Only for scripts/register-livepix-webhook.mjs (one-time after deploy)
LIVEPIX_WEBHOOK_URL=https://pergunteaopolvo.com/api/webhooks/livepix
